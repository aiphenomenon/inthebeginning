# Cosmic Runner V5 (Legacy)

> **Note**: This is a legacy development copy. The current game is
> `inthebeginning bounce` in `deploy/v10/inthebeginning-bounce/`.
> Development happens via `apps/inthebeginning-bounce` (symlink).

This directory contains an intermediate version of the game from the
V5 era. It was used as a development staging area before the deploy/
directory structure was established.
